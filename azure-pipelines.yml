# HTML
# Archive your static HTML project and save it with the build record.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.6'
- script: sudo apt-get -y install python3-pip && python3 -m pip3 install --upgrade pip3 setuptools wheel tabula-py && python3 -m pdb pdftojson.ipynb